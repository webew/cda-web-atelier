{% extends 'base.html.twig' %}

{% block title %}Materiel index{% endblock %}

{% block body %}
<h1>Gestion des matériels</h1>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Designation</th>
            <th>RefConstructeur</th>
            <th>NumSerie</th>
            <th>actions</th>
        </tr>
    </thead>
    <tbody>
        {% for materiel in materiels %}
        <tr>
            <td>{{ materiel.id }}</td>
            <td>{{ materiel.designation }}</td>
            <td>{{ materiel.refConstructeur }}</td>
            <td>{{ materiel.numSerie }}</td>
            <td>
                <a href="{{ path('app_materiel_show', {'id': materiel.id}) }}">show</a>
                <a href="{{ path('app_materiel_edit', {'id': materiel.id}) }}">edit</a>
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="5">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>

<a href="{{ path('app_materiel_new') }}">Create new</a>
{% endblock %}